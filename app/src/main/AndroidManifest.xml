<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lauwba.pelaporan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".config.PrefsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".TampilTl"></activity>
        <activity android:name=".ListViewTL" />
        <activity android:name=".ViewTowerLamp" />
        <activity android:name=".InputTowerLamp" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Tambah" />
        <activity android:name=".Profil" />
        <activity android:name=".Data" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Register" />
    </application>

</manifest>